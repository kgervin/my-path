# My Path web

React + TypeScript SPA. See the [root README](../README.md).

```bash
npm ci
npm run dev      # http://localhost:5173, proxies /api to :8000
npm test         # Vitest + axe
npm run e2e      # Playwright + axe (phone and desktop)
```
