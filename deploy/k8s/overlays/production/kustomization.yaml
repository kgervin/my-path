apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: my-path
resources:
  - ../../base
patches:
  - target: { kind: HorizontalPodAutoscaler, name: my-path-api }
    patch: |-
      - op: replace
        path: /spec/minReplicas
        value: 3
  - target: { kind: Deployment, name: my-path-api }
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
