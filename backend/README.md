# My Path API

FastAPI service. See the [root README](../README.md) and [architecture](../docs/architecture.md).

```bash
uv sync
uv run uvicorn my_path.main:create_app --factory --reload
uv run pytest
```
